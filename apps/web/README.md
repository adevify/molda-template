# Web application

Reserved for the first application UI selected during architecture. Approved pages and
components will later be connected here through typed bindings; do not duplicate their
presentation during preview.

After the build gate opens, follow the React/MUI, static UI, security, and testing
contracts routed from [`../../docs/README.md`](../../docs/README.md). Do not duplicate
the preview presentation tree and do not add network access to shared pages or
components.
