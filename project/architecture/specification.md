# Architecture specification

Status: blocked-by-design-approval

After approval and the business-flow survey, record apps, UIs, API contracts, entities,
external infrastructure bindings, selected modules, glue code, tests, migrations, and
the single-image runtime here before implementation.
